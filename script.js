let person = "Guilherme";
var exp = 30001;

  while(exp <= 1000) {
    var exp = "Ferro";
  }
  while(exp >= 1001 && exp <= 2000) {
    var exp = "Bronze";
  }
  while(exp >= 2001 && exp <= 5000) {
    var exp = "Prata";
  }
  while(exp >= 6001 && exp <= 7000) {
    var exp = "Ouro";
  }
  while(exp >= 7001 && exp <= 8000) {
    var exp = "Platina";
  }
  while(exp >= 8001 && exp <= 9000) {
    var exp = "Ascendente";
  }
  while(exp >= 9001 && exp <= 10000) {
    var exp = "Imortal";
  }
  while(exp >= 10001) {
      var exp = "Radiante";
  }
  
console.log("O herói de nome " + person + " Está no nível " + exp)